name: staging-deploy

on:
  pull_request:
    types: [ labeled ]

jobs:
  call-workflow-build-test:
   uses: ./.github/workflows/push-docker-img-reusable-worflow.yaml
   if: ${{ github.event.label.name == 'deploy-staging' }}
#  call-workflow-staging-gcp-deploy:
